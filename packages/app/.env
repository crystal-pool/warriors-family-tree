# DO NOT put secrets into this file.
# Instead, create your own `.env.local` file (which is gitignored) and override the values here.

# Mangled AppInsights connection string (salt-then-base64).
# Not storing the plain text to avoid getting scraped.
# See telemetry.ts for how it's postprocessed.
VITE_AI_CONNECTION_STRING_EXPR=
