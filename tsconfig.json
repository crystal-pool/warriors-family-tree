// Root tsconfig for project references. Enables:
// 1. `tsc --build` from repo root to type-check all packages in one shot (CI).
// 2. VS Code language server cross-package reference resolution.
{
  "files": [],
  "references": [
    { "path": "./packages/shared" },
    { "path": "./packages/app" },
    { "path": "./packages/embed" }
  ]
}